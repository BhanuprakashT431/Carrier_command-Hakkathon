# Backend API base URL (proxied through Vite in dev via /api)
VITE_API_URL=http://localhost:5000/api

# Multi-agent orchestration service URL
VITE_AGENTS_URL=http://localhost:8000

# Set to 'true' to enable the Demo Mode banner and use synthetic data
VITE_DEMO_MODE=true
